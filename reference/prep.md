# prepare a dataset by applying a pre-processing pipeline

prepare a dataset by applying a pre-processing pipeline

## Usage

``` r
prep(x, ...)
```

## Arguments

- x:

  the pipeline

- ...:

  extra args

## Value

the pre-processed data
