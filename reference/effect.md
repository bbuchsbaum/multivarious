# Extract a named effect from a fitted model

Extract a named effect from a fitted model

## Usage

``` r
effect(x, term, ...)
```

## Arguments

- x:

  A fitted model object.

- term:

  A term label or one-sided formula identifying the effect of interest.

- ...:

  Additional arguments passed to methods.

## Value

An effect object, typically an `effect_operator`.
