# reverse a pre-processing transform

reverse a pre-processing transform

## Usage

``` r
reverse_transform(x, X, colind, ...)
```

## Arguments

- x:

  the pre_processor

- X:

  the data matrix

- colind:

  column indices

- ...:

  extra args

## Value

the reverse-transformed data
