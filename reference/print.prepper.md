# Print a prepper pipeline

Uses `cli` to produce a colorful and readable representation of the
pipeline steps.

## Usage

``` r
# S3 method for class 'prepper'
print(x, ...)
```

## Arguments

- x:

  A `prepper` object.

- ...:

  Additional arguments (ignored).
