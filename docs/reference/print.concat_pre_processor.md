# Print a concat_pre_processor object

Print a concat_pre_processor object

## Usage

``` r
# S3 method for class 'concat_pre_processor'
print(x, ...)
```

## Arguments

- x:

  A `concat_pre_processor` object.

- ...:

  Additional arguments (ignored).
