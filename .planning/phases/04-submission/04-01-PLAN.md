---
phase: 04-submission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All package changes are committed to git"
    - "Fresh R CMD check passes with 0E/0W/0N"
    - "cran-comments.md accurately reflects current check results"
    - "DESCRIPTION metadata is correct for submission"
  artifacts:
    - path: "DESCRIPTION"
      provides: "Package metadata"
      contains: "Version: 0.3.0"
    - path: "cran-comments.md"
      provides: "CRAN submission notes"
      contains: "0 errors | 0 warnings | 0 notes"
    - path: "NEWS.md"
      provides: "Changelog"
      contains: "multivarious 0.3.0"
  key_links:
    - from: "R/*.R"
      to: "git"
      via: "committed changes"
      pattern: "working tree clean"
---

<objective>
Prepare the multivarious package for CRAN submission by committing all Phase 1-3 fixes and running a fresh R CMD check to verify the package still passes.

Purpose: CRAN submission requires a clean git state and verified check results. The uncommitted changes in git are the actual fixes from Phases 1-3 that need to be committed.

Output: Clean git repository with fresh R CMD check confirming 0E/0W/0N status.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-submission/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit all Phase 1-3 fixes</name>
  <files></files>
  <action>
The uncommitted changes in git (R/*.R, tests/*.R, vignettes/*.Rmd) are the actual fixes from Phases 1-3. These MUST be committed before CRAN submission.

1. Run `git status` to inventory all changes
2. Review that changes align with documented Phase 1-3 work:
   - R/pca.R, R/cPCA.R, R/bi_projector.R, R/pre_process.R, R/nystrom_embedding.R - Phase 1-3 fixes
   - tests/* - test file updates from Phase 1
   - vignettes/*.Rmd - YAML header and albersdown fixes from Phase 2-3
3. Stage all changes: `git add R/ tests/ vignettes/`
4. Do NOT stage docs/ (pkgdown output, excluded by .Rbuildignore)
5. Commit with message summarizing v0.3.0 changes:
   "fix: prepare multivarious v0.3.0 for CRAN submission

   - Fixed T/F shorthand to TRUE/FALSE (REQ-001)
   - Converted dontrun to donttest for executable examples (REQ-002)
   - Fixed bootstrap.plsc duplicate argument handling
   - Fixed S3 method registration
   - Added missing imports (coefficients, combn)
   - Fixed vignette YAML headers
   - Fixed requireNamespace parameter (R-devel)
   - Removed escaped underscores and albersdown references"
  </action>
  <verify>
`git status` shows clean working tree for R/, tests/, vignettes/ directories.
`git log -1 --oneline` shows the new commit.
  </verify>
  <done>All Phase 1-3 code changes are committed to git. Only untracked check artifacts and docs/ remain.</done>
</task>

<task type="auto">
  <name>Task 2: Run fresh R CMD check</name>
  <files></files>
  <action>
Run a fresh R CMD check immediately before submission. Do NOT trust cached results.

1. Clean any existing check artifacts:
   ```bash
   rm -rf multivarious.Rcheck check.log check_output.log
   ```

2. Run R CMD check with CRAN settings:
   ```r
   devtools::check(args = c("--as-cran"))
   ```
   Or via command line:
   ```bash
   R CMD build . && R CMD check --as-cran multivarious_0.3.0.tar.gz
   ```

3. Verify result is exactly: 0 errors | 0 warnings | 0 notes

4. If any issues arise, report immediately - do NOT proceed with submission.
  </action>
  <verify>
R CMD check output shows "0 errors | 0 warnings | 0 notes" or equivalent.
Check log does not contain ERROR, WARNING, or NOTE sections with content.
  </verify>
  <done>Fresh R CMD check passes with 0E/0W/0N on local machine.</done>
</task>

<task type="auto">
  <name>Task 3: Final metadata verification</name>
  <files></files>
  <action>
Verify all submission metadata files are accurate:

1. DESCRIPTION verification:
   - Version: 0.3.0
   - Maintainer email: brad.buchsbaum@gmail.com (must be valid, will receive confirmation)
   - URL: https://bbuchsbaum.github.io/multivarious/ (must be accessible)

2. cran-comments.md verification:
   - R CMD check results section says "0 errors | 0 warnings | 0 notes"
   - Test environments list is accurate and matches what was tested
   - Resubmission section accurately describes changes

3. NEWS.md verification:
   - Has "# multivarious 0.3.0" header (CRAN-compliant format)
   - Documents all user-facing changes from v0.2.0 to v0.3.0

4. .Rbuildignore verification:
   - Contains ^\.planning$
   - Contains ^CLAUDE\.md$
   - Contains ^check\.log$
   - Contains ^multivarious\.Rcheck$

Report any discrepancies found. If all correct, state "All metadata verified for submission."
  </action>
  <verify>
All metadata files contain expected content.
No discrepancies between documented check results and actual check results.
  </verify>
  <done>DESCRIPTION, cran-comments.md, NEWS.md, and .Rbuildignore all verified accurate.</done>
</task>

</tasks>

<verification>
Pre-submission checklist complete:
- [ ] Git working tree clean (R/, tests/, vignettes/ committed)
- [ ] Fresh R CMD check: 0 errors | 0 warnings | 0 notes
- [ ] DESCRIPTION Version: 0.3.0
- [ ] Maintainer email verified
- [ ] cran-comments.md matches actual check results
- [ ] NEWS.md documents v0.3.0 changes
- [ ] .Rbuildignore excludes dev artifacts
</verification>

<success_criteria>
1. Git status shows committed changes (only untracked artifacts and docs/ remain)
2. Fresh R CMD check returns 0E/0W/0N
3. All metadata files verified accurate
4. Package is ready for devtools::submit_cran()
</success_criteria>

<output>
After completion, create `.planning/phases/04-submission/04-01-SUMMARY.md`
</output>
