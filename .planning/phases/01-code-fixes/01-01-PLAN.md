---
phase: 01-code-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/svd.R
  - R/all_generic.R
  - R/geneig.R
  - R/multiblock.R
autonomous: true
user_setup: []

must_haves:
  truths:
    - "R CMD check examples run without 'must be a matrix' errors"
    - "R CMD check reports no non-ASCII character warnings"
  artifacts:
    - path: "R/svd.R"
      provides: "Fixed example using as.matrix()"
      contains: "as.matrix(iris"
    - path: "R/all_generic.R"
      provides: "Fixed example using as.matrix()"
      contains: "as.matrix(iris"
    - path: "R/geneig.R"
      provides: "ASCII-only roxygen documentation"
    - path: "R/multiblock.R"
      provides: "ASCII-only roxygen documentation"
  key_links:
    - from: "R/svd.R example"
      to: "svd_wrapper function"
      via: "X matrix input"
      pattern: "as\\.matrix\\(iris"
---

<objective>
Fix critical blocking issues that prevent R CMD check examples from running.

Purpose: R CMD check fails at the examples stage due to data frame vs matrix type errors and non-ASCII character warnings. These must be fixed first before any other checks can complete.

Output: Fixed R files with examples that pass and ASCII-only documentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-fixes/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix data frame to matrix conversion in examples</name>
  <files>R/svd.R, R/all_generic.R</files>
  <action>
Fix the example code in two files where `iris[, 1:4]` returns a data frame but a matrix is required:

1. **R/svd.R line 25**: Change `X <- iris[, 1:4]` to `X <- as.matrix(iris[, 1:4])`

2. **R/all_generic.R line 408**: Change `X <- iris[, 1:4]` to `X <- as.matrix(iris[, 1:4])`

These are in roxygen `@examples` blocks. The function `svd_wrapper` requires a matrix input (enforced by `chk::chk_matrix(X)`), so examples must provide matrices.
  </action>
  <verify>
Run: `grep -n "as.matrix(iris" R/svd.R R/all_generic.R`
Expected: Both files show the corrected pattern with `as.matrix(iris[, 1:4])`
  </verify>
  <done>Both example blocks use `as.matrix()` to convert iris data to matrix format</done>
</task>

<task type="auto">
  <name>Task 2: Replace non-ASCII characters with ASCII or Unicode escapes</name>
  <files>R/geneig.R, R/multiblock.R</files>
  <action>
Replace non-ASCII characters in roxygen documentation with ASCII equivalents or Unicode escapes:

**R/geneig.R replacements:**
- Line 40: Replace `lambda` symbol with `lambda` (word) or `\\u03BB`
- Line 56: Replace arrow symbol with `->`
- Line 65: Replace section symbol and em-dash with ASCII equivalents
- Line 69: Replace en-dash/em-dash with `-` or `--`
- Line 73: Replace en-dash/em-dash with `-` or `--`
- Line 336: Replace mu and lambda symbols with ASCII or `\\u03BC`/`\\u03BB`
- Lines 477, 484-486: Replace lambda/mu symbols with ASCII text

**R/multiblock.R replacements:**
- Lines 207, 214: Replace em-dash with `--` or `-`
- Line 211: Replace multiplication sign with `x` or `*`
- Lines 227, 230-231: Replace em-dash and multiplication sign
- Line 267: Replace ellipsis with `...`
- Lines 297, 306: Replace en-dash with `-`
- Line 337: Replace en-dash with `-`
- Lines 384, 408, 411: Replace multiplication sign with `x` or `*`
- Lines 483, 492: Replace en-dash with `-`

Use descriptive ASCII text where symbols appear in documentation (e.g., "lambda" instead of the Greek letter). For comments that are purely internal notes, em-dashes can become `--`.
  </action>
  <verify>
Run in R: `tools::showNonASCIIfile('R/geneig.R')` and `tools::showNonASCIIfile('R/multiblock.R')`
Expected: Both commands return no output (no non-ASCII characters found)
  </verify>
  <done>Both R/geneig.R and R/multiblock.R contain only ASCII characters</done>
</task>

</tasks>

<verification>
After completing both tasks:

1. Run R CMD check to verify examples now execute:
   ```bash
   R --vanilla --quiet -e "devtools::check()" 2>&1 | grep -A5 "checking examples"
   ```
   Expected: No "must be an matrix" error

2. Verify non-ASCII warning is resolved:
   ```bash
   R --vanilla --quiet -e "devtools::check()" 2>&1 | grep -i "non-ASCII"
   ```
   Expected: No "non-ASCII characters" warning
</verification>

<success_criteria>
- R CMD check examples section runs without type errors
- R CMD check reports no non-ASCII character warnings
- Both fixed files pass `tools::showNonASCIIfile()` check
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-fixes/01-01-SUMMARY.md`
</output>
