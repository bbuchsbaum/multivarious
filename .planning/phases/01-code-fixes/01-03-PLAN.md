---
phase: 01-code-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - R/bi_projector.R
  - R/multiblock.R
  - DESCRIPTION
autonomous: true
user_setup: []

must_haves:
  truths:
    - "R CMD check reports no 'Undefined global functions' notes for coefficients and combn"
    - "R CMD check reports no 'Namespace in Imports field not imported' note for tidyr"
  artifacts:
    - path: "R/bi_projector.R"
      provides: "Proper import for coefficients"
      contains: "@importFrom stats coefficients"
    - path: "R/multiblock.R"
      provides: "Proper import for combn"
      contains: "@importFrom utils combn"
    - path: "DESCRIPTION"
      provides: "Cleaned Imports without tidyr"
  key_links:
    - from: "R/bi_projector.R"
      to: "stats::coefficients"
      via: "importFrom directive"
      pattern: "importFrom.*stats.*coefficients"
---

<objective>
Fix missing function imports and remove unused package import.

Purpose: R CMD check reports NOTEs about undefined global functions (coefficients, combn) and an unused import (tidyr). These must be resolved for a clean CRAN submission.

Output: Proper importFrom directives and cleaned DESCRIPTION file.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-fixes/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing importFrom directives</name>
  <files>R/bi_projector.R, R/multiblock.R</files>
  <action>
Add proper `@importFrom` roxygen directives for the undefined global functions:

1. **R/bi_projector.R**: Find the `reconstruct.bi_projector` function and add to its roxygen block:
```r
#' @importFrom stats coefficients
```
The function uses `coefficients()` which is from the stats package.

2. **R/multiblock.R**: Find the `perm_test.multiblock_projector` function and locate its `default_measure` internal function. Add to the parent function's roxygen block:
```r
#' @importFrom utils combn
```
The `combn` function is used inside `default_measure` which computes pairwise correlations.

After adding these, run `devtools::document()` to update NAMESPACE.
  </action>
  <verify>
Run: `grep -E "@importFrom.*(coefficients|combn)" R/bi_projector.R R/multiblock.R`
Expected: Both files show the appropriate importFrom directives
  </verify>
  <done>Both coefficients and combn have proper @importFrom directives</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused tidyr from Imports</name>
  <files>DESCRIPTION</files>
  <action>
Edit DESCRIPTION file to remove `tidyr` from the Imports field.

The current Imports line includes tidyr, but R CMD check indicates it's not actually used in the package code. Remove it to eliminate the NOTE.

Find the `Imports:` section and remove the `tidyr,` entry (including the comma/line as appropriate to maintain proper formatting).

Note: Verify tidyr is indeed unused by checking if any R files have `@import tidyr` or `tidyr::` calls. If it is used somewhere, do NOT remove it - instead add the appropriate @import directive.
  </action>
  <verify>
Run: `grep -r "tidyr" R/` to confirm tidyr is not used in R code
Run: `grep "tidyr" DESCRIPTION` to confirm it's removed from Imports
Expected: No tidyr references in R/ code, and tidyr not in DESCRIPTION Imports
  </verify>
  <done>tidyr removed from DESCRIPTION Imports field</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate NAMESPACE</name>
  <files>NAMESPACE</files>
  <action>
Run `devtools::document()` to regenerate NAMESPACE with the new importFrom directives.

The NAMESPACE should now include:
- `importFrom(stats,coefficients)`
- `importFrom(utils,combn)`

And tidyr should no longer be imported.
  </action>
  <verify>
Run in R: `devtools::document()`
Then: `grep -E "importFrom.*(coefficients|combn)" NAMESPACE`
Expected: Both importFrom directives appear in NAMESPACE
  </verify>
  <done>NAMESPACE includes proper importFrom directives for coefficients and combn</done>
</task>

</tasks>

<verification>
After completing all tasks, run:

```bash
R --vanilla --quiet -e "devtools::check()" 2>&1 | grep -E "(Undefined global|Namespace in Imports)"
```

Expected: No output (no undefined global functions or unused imports)
</verification>

<success_criteria>
- No "Undefined global functions or variables: coefficients combn" NOTE
- No "Namespace in Imports field not imported from: tidyr" NOTE
- NAMESPACE contains importFrom directives for both functions
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-fixes/01-03-SUMMARY.md`
</output>
