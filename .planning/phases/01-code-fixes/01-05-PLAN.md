---
phase: 01-code-fixes
plan: 05
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03, 01-04]
files_modified: []
autonomous: false
user_setup: []

must_haves:
  truths:
    - "R CMD check reports 0 errors"
    - "R CMD check reports 0 warnings"
    - "All tests pass via devtools::test()"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 1 fixes by running final R CMD check and tests.

Purpose: Confirm that all code fixes from plans 01-04 result in a clean R CMD check with zero errors and zero warnings, satisfying REQ-003, REQ-004, and REQ-007.

Output: Verification of clean R CMD check output and passing tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-fixes/01-01-SUMMARY.md
@.planning/phases/01-code-fixes/01-02-SUMMARY.md
@.planning/phases/01-code-fixes/01-03-SUMMARY.md
@.planning/phases/01-code-fixes/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run devtools::test() to verify all tests pass</name>
  <files></files>
  <action>
Run the test suite to verify all unit tests pass:

```r
devtools::test()
```

All tests should pass. If any tests fail:
1. Note the failing test(s)
2. Determine if the failure is related to Phase 1 changes
3. If related, fix the issue
4. If unrelated, document for later phases
  </action>
  <verify>
Run: `R --vanilla --quiet -e "devtools::test()"`
Expected: "[ FAIL 0 | WARN 0 | SKIP n | PASS m ]" with FAIL = 0
  </verify>
  <done>All tests pass with zero failures</done>
</task>

<task type="auto">
  <name>Task 2: Run devtools::check() for final verification</name>
  <files></files>
  <action>
Run full R CMD check to verify all Phase 1 requirements are met:

```r
devtools::check()
```

Check the output for:
- 0 errors
- 0 warnings
- NOTEs should be acceptable (e.g., hidden files like .planning, non-standard files like CLAUDE.md)

Document the exact check output including any NOTEs.
  </action>
  <verify>
Run: `R --vanilla --quiet -e "devtools::check()"`
Expected: Output shows "0 errors | 0 warnings | N notes" where N is acceptable
  </verify>
  <done>R CMD check passes with 0 errors and 0 warnings</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 1 code fixes addressing:
- Data frame to matrix conversion in examples
- Non-ASCII character replacement
- S3 method registration and signature fixes
- Missing imports
- Unused import removal
- \\dontrun{} to \\donttest{} conversion
- Documentation of missing arguments
  </what-built>
  <how-to-verify>
1. Review the R CMD check output
2. Confirm 0 errors
3. Confirm 0 warnings
4. Review any NOTEs to ensure they are acceptable:
   - Hidden files/directories (.claude, .planning) - acceptable, not included in CRAN package
   - Non-standard files (CLAUDE.md) - acceptable, not included in CRAN package
   - Suggested packages not available - acceptable if they're optional
5. Confirm tests pass

If any errors or warnings remain, describe the issues.
  </how-to-verify>
  <resume-signal>Type "approved" if check passes with 0 errors and 0 warnings, or describe remaining issues</resume-signal>
</task>

</tasks>

<verification>
Final verification checklist:

1. **REQ-001** (T/F shorthand): Already verified as fixed in research
2. **REQ-002** (\\dontrun{} misuse): Converted to \\donttest{} in Plan 04
3. **REQ-003** (Zero errors): Verified by devtools::check()
4. **REQ-004** (Zero warnings): Verified by devtools::check()
5. **REQ-007** (All tests pass): Verified by devtools::test()
</verification>

<success_criteria>
- devtools::test() reports 0 failures
- devtools::check() reports 0 errors
- devtools::check() reports 0 warnings
- All 5 Phase 1 requirements (REQ-001 through REQ-004, REQ-007) are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-fixes/01-05-SUMMARY.md`
</output>
