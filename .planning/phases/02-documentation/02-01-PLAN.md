---
phase: 02-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .Rbuildignore
  - DESCRIPTION
  - NEWS.md
autonomous: true

must_haves:
  truths:
    - "Package build excludes development artifacts (.planning, .claude, CLAUDE.md)"
    - "Version shows 0.3.0 in DESCRIPTION"
    - "NEWS.md documents all changes since 0.2.0"
  artifacts:
    - path: ".Rbuildignore"
      provides: "Exclusion patterns for development files"
      contains: "^\\.planning$"
    - path: "DESCRIPTION"
      provides: "Package metadata with updated version"
      contains: "Version: 0.3.0"
    - path: "NEWS.md"
      provides: "Changelog for CRAN and users"
      contains: "# multivarious 0.3.0"
  key_links:
    - from: "NEWS.md"
      to: "DESCRIPTION"
      via: "version consistency"
      pattern: "0\\.3\\.0"
---

<objective>
Update .Rbuildignore, bump version to 0.3.0, and create NEWS.md documenting all changes since v0.2.0.

Purpose: Prepare package metadata for CRAN submission by excluding development files, setting correct version, and documenting changes.
Output: Clean .Rbuildignore, DESCRIPTION with 0.3.0, and complete NEWS.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-documentation/02-RESEARCH.md

Reference files:
@.Rbuildignore
@DESCRIPTION
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .Rbuildignore to exclude development artifacts</name>
  <files>.Rbuildignore</files>
  <action>
Add the following patterns to .Rbuildignore to exclude development artifacts from the package tarball:

```
^\.planning$
^\.claude$
^CLAUDE\.md$
^figure$
^check\.log$
^README\.html$
```

These patterns use proper regex anchoring (^ for start). The dot in file extensions must be escaped (\\.).

Do NOT remove existing patterns. Append these new patterns after the existing `.cursor` line.
  </action>
  <verify>
Run: `grep -E "planning|claude|CLAUDE|figure|check\.log|README\.html" .Rbuildignore`
Should show all 6 new patterns.

Run: `R CMD build . 2>&1 | grep -E "planning|claude|CLAUDE"`
Should show NO matches (files excluded from build).
  </verify>
  <done>
.Rbuildignore contains patterns for .planning, .claude, CLAUDE.md, figure, check.log, and README.html. R CMD build excludes these files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump version in DESCRIPTION to 0.3.0</name>
  <files>DESCRIPTION</files>
  <action>
Edit DESCRIPTION file to change:

```
Version: 0.2.0
```

to:

```
Version: 0.3.0
```

This is a standard minor version increment for a resubmission with bug fixes and internal changes.
  </action>
  <verify>
Run: `grep "^Version:" DESCRIPTION`
Should output: `Version: 0.3.0`
  </verify>
  <done>
DESCRIPTION shows Version: 0.3.0
  </done>
</task>

<task type="auto">
  <name>Task 3: Create NEWS.md documenting v0.3.0 changes</name>
  <files>NEWS.md</files>
  <action>
Create NEWS.md in the package root with the following content. Use the exact heading format `# multivarious 0.3.0` (required for R's utils::news() parser).

Document the following changes from Phase 1 work:

Bug Fixes:
- Fixed T/F shorthand to TRUE/FALSE in pca() for CRAN compliance
- Converted \dontrun{} to \donttest{} for executable but slow examples
- Fixed bootstrap.plsc() duplicate argument handling when called with named X/Y arguments
- Fixed regress() PLS method dimension mismatch
- Fixed iris data frame to matrix conversion in examples

Internal Changes:
- Registered S3 methods: classifier.projector, inverse_projection.projector, perm_ci.pca
- Added missing importFrom directives for coefficients and combn
- Replaced non-ASCII characters with ASCII equivalents in documentation

Deprecated:
- prep() deprecated in favor of fit() for preprocessing pipelines
- perm_ci.pca() deprecated
- perm_test.plsc() deprecated

Also include a section for v0.2.0 (Initial CRAN release) for completeness.
  </action>
  <verify>
Run: `head -20 NEWS.md`
Should show properly formatted changelog starting with `# multivarious 0.3.0`

Run: `Rscript -e "utils::news(package='multivarious', lib.loc='.')"`
If NEWS.md format is correct, this should parse without error (may show "no news" if not installed, but no parse error).
  </verify>
  <done>
NEWS.md exists with proper heading format, documents Bug Fixes, Internal Changes, and Deprecated sections for v0.3.0, and includes v0.2.0 entry.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. .Rbuildignore verification:
   ```bash
   grep -c "planning\|claude\|CLAUDE\|figure\|check\.log\|README\.html" .Rbuildignore
   ```
   Should return 6.

2. Version verification:
   ```bash
   grep "^Version:" DESCRIPTION
   ```
   Should return `Version: 0.3.0`.

3. NEWS.md verification:
   ```bash
   head -1 NEWS.md
   ```
   Should return `# multivarious 0.3.0`.

4. Build excludes development files:
   ```bash
   R CMD build . 2>&1 | head -10
   ```
   Should NOT mention .planning, .claude, CLAUDE.md in package contents.
</verification>

<success_criteria>
1. .Rbuildignore contains all 6 new exclusion patterns
2. DESCRIPTION Version field is 0.3.0
3. NEWS.md exists with proper CRAN-compliant heading format
4. NEWS.md documents all Phase 1 changes (bug fixes, internal, deprecated)
5. R CMD build excludes development artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/02-documentation/02-01-SUMMARY.md`
</output>
